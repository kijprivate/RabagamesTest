using UnityEngine;
using System.Collections;

public class DragItemsToCorrectPositionPiece : MonoBehaviour {
	// This is a puzzle type where the player drags pieces to the correct slots.  
	// As opposed to NamePlatePiece, multiple pieces can be the identical, so they can both be correct on the slot

	public int itemID;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
